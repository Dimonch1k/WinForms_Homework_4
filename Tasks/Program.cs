using System;
using Task1;
using Task2;
using Task3;
using Task4;
using Task5;

namespace Tasks
{
    public static class Program
    {
        static void Main(string[] args)
        {
            // =============================================================
            //ShowMessage showMessage = new ShowMessage();
            //Console.WriteLine(showMessage.ShowInfoMessage());

            // =============================================================
            //LeapYear leapYear = new LeapYear();
            //Console.WriteLine(leapYear.IsLeapYear(2000));
            //Console.WriteLine(leapYear.IsLeapYear(1900));
            //Console.WriteLine(leapYear.IsLeapYear(2024));

            // =============================================================
            //MinMaxSum calc = new MinMaxSum();

            //int num1 = 10, num2 = 20, num3 = 15;
            //Console.WriteLine($"Numbers: {num1}, {num2}, {num3}");
            //Console.WriteLine(calc.GetMinMaxSum(num1, num2, num3));

            // =============================================================
            //NumberHelper numHelper = new NumberHelper();
            //int number = 7;

            //int factorial = numHelper.Factorial(number);
            //bool isPrime = numHelper.IsPrime(number);
            //bool isEven = numHelper.IsEven(number);
            //bool isOdd = numHelper.IsOdd(number);

            //Console.WriteLine($"Factorial of {number} is: {factorial}");
            //Console.WriteLine($"{number} is prime: {isPrime}");
            //Console.WriteLine($"{number} is even: {isEven}");
            //Console.WriteLine($"{number} is odd: {isOdd}");

            // =============================================================
            //Fraction fraction1 = new Fraction(1, 2);
            //Fraction fraction2 = new Fraction(3, 4);

            //Fraction sum = Fraction.Add(fraction1, fraction2);
            //Fraction difference = Fraction.Subtract(fraction1, fraction2);
            //Fraction product = Fraction.Multiply(fraction1, fraction2);
            //Fraction quotient = Fraction.Divide(fraction1, fraction2);

            //Console.WriteLine($"Sum: {sum}");
            //Console.WriteLine($"Difference: {difference}");
            //Console.WriteLine($"Product: {product}");
            //Console.WriteLine($"Quotient: {quotient}");


            Console.ReadKey();
        }
    }
}
